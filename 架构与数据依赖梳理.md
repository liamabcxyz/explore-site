# VANTAGE 架构与数据依赖梳理

**目的**：在把服务部署上线、并考虑把一部分功能挪到 Cloudflare Workers 之前，先把"我们目前有什么功能 / 用了什么数据 / 数据从哪里来 / 依赖了什么"一次讲清楚。文档最后附一节"迁移候选分析"，供后续决策参考——但**不做决策**，只列事实。

生成时间：2026-09-02。对应 commit `029f026e`（最新 main）。

---

## TL;DR

- **VANTAGE 是纯静态站点**（Next.js `output: "export"`），所有计算和数据加载都跑在浏览器里。
- **重活儿在两处**：主线程的 corridor 建筑抓取+解码（PMTiles+PBF+MVT），Web Worker 里的 viewshed 计算。
- **外部服务共 4 个**：Overture STAC 目录、Overture PMTiles（间接）、AWS Terrain Tiles、`geocoder.bradr.dev`（第三方地理搜索）。此外还有 Google Fonts。
- **无后端、无 auth、无用户数据服务器**。Report-a-problem 目前只能 copy/download，写入 clipboard 或本地文件。
- **明显可迁移的候选**（不做建议，只列事实）：报告收集端点（当前不存在）、STAC 目录解析（8 次网络往返可压缩成 1 次）、走廊建筑解码（可以把 PMTiles+PBF+MVT 从浏览器拿掉）、地理搜索代理（当前依赖第三方主机）。

---

## 一、功能清单

按当前上线可见性分类。文件路径给出关键锚点，`:N` 是行号。

### A. 生产环境可见

| 功能 | 文件 | 一句话 |
|---|---|---|
| 落地页 | `app/page.jsx` + `components/landing/CitySearch.jsx` | VANTAGE 品牌 + 城市搜索 + 建议城市 chips，选择后跳 `/map#…`。也是 `#z/lat/lng` hash 的转跳器。 |
| Header | `components/nav/Header.jsx` | 固定 AppBar：wordmark、`ShareButton`（复制当前 URL）、`DarkModeToggle`、`SearchBox` |
| Search widget | `components/nav/SearchBox.jsx` | 右上下拉三模式：Locality（默认，地名）/ Country / GERS（Overture 内部 ID）。全部打 `geocoder.bradr.dev`。 |
| BookmarkDial | `components/BookmarkDial.jsx` | 左下星形 FAB，展开 4 个硬编码相机预设（Paris/NYC/London/Boston）。**只是相机跳转**，不设 launch point。 |
| Feature-inspect popup | `components/FeatureSelector.jsx` + `MapView.jsx:290-368` | 点击有多个 Overture feature 叠在一起时弹选择框；单 feature 点击只高亮 + 写 URL 的 `?feature=` 参数。 |
| 地图本体 | `MapView.jsx` + `lib/LayerManager.js` + `components/map/*` | MapLibre 地球/墨卡托切换，PMTiles 6 主题，`NavigationControl`/`GeolocateControl`/`TerrainToggleControl`/`ScaleControl`/`AttributionControl` |
| Launch point 分析 | `components/launch/LaunchPointControl.jsx` | VANTAGE 核心：放发射点、放观察点、caliber 滑块、rooftop 层开关、真烟花秀 preset 下拉、legend、清除。 |
| Profile Dock | `components/analysis/ProfileDock.jsx` + `ProfilePanel.jsx` | 底部固定 dock：verdict 标题栏 + SkylineChart（仰角剖面图）+ PerspectivePreview（3D 迷你场景）+ Bug 按钮。 |
| Report a problem | `components/analysis/ReportProblemDialog.jsx` + `lib/debug/{bundle,trace,annotations,annotationLayer}.js` | Bug 图标打开对话框，收集：什么不对（textarea）、期望结果（3 按钮）、地图上标注（最多 3 处）、其它上下文。生成 JSON，走 clipboard 或 download。 |
| URL state | `lib/launchUrlState.js` + MapLibre `hash:true` | 一份 URL 完整还原：launch/observer/caliber/level/floor/rooftop/hillshade/preset/stac 十几项参数 + camera |
| Terms of Use | `components/nav/TermsOfUse.jsx` | 底部小 pill 打开对话框。**文案还写着 "Overture Explorer"，跟 VANTAGE 品牌不一致，待修**。 |
| Firework 动画 | `LaunchPointControl.jsx` 的 `FIREWORK_CSS` + `buildFireworkHtml` | 放 launch point 时 60 粒子 CSS 动画。**顶点高度硬编码 140px，跟口径无关（todo.md 已记）**。 |

### B. Flag-gated（代码在，flag off）

| 功能 | Flag | 文件 |
|---|---|---|
| Layer 树编辑器 + SidePanel 三 tab（Explore/Inspect/Features） | `ENABLE_LAYER_TABS=false` @ `SidePanel.jsx:16` | `LayerTree.jsx` |
| Inspect-compare（左右对比双 MapLibre + 拖动滑块） | `ENABLE_INSPECT_COMPARE=false` @ `MapView.jsx:89` | `MapView.jsx:389-413` |

### C. Dev-only（NODE_ENV != production 时挂载）

| 功能 | 文件 |
|---|---|
| PerfOverlay（右下 FPS + viewshed timing HUD） | `components/launch/PerfOverlay.jsx`，挂载 @ `app/map/page.jsx:185` |

### D. 死代码（源码里有，Header 没接进来）

- `components/nav/GithubButton.jsx`
- `components/nav/LanguageSwitcher.jsx`

---

## 二、数据来源

浏览器打到的每一个外部主机 + 自托管资源。

### 2.1 外部服务

| 主机 | 目的 | 每次会话流量 | 缓存 | 谁在调 | 失败后果 |
|---|---|---|---|---|---|
| `stac.overturemaps.org` | Overture STAC 目录，用来发现各主题最新的 PMTiles URL | 冷启：30~80 KB（1+1+6 次目录/主题 JSON），热启：~4 KB | `localStorage["overture-stac-cache"]`，key 是 release URL | 主线程 `lib/stacService.js`，`fetch()`（现已被 `traceFetch` 包） | 地图无数据源，`console.error`，无用户可见错误 |
| Overture PMTiles S3 archive（形如 `overturemaps-tiles-*-beta.s3.amazonaws.com`） | 6 个主题（base/buildings/places/divisions/transportation/addresses）的 vector tiles | 5~50 MB（随平移缩放） | MapLibre 内部瓦片缓存 | 3 处：MapLibre 主实例 + `lib/HiddenBuildingSource.js`（第二个隐形 MapLibre 实例，用来查视口外的建筑） + `lib/geo/corridorBuildings.js:100` 直接 `PMTiles.getZxy()`（走廊分析） | 空地图 / 分析静默降级为"路径上没有建筑" |
| `s3.amazonaws.com/elevation-tiles-prod/terrarium/{z}/{x}/{y}.png` | AWS Open Data / Mapzen 地形，Terrarium PNG 编码，`height = (R·256 + G + B/256) − 32768` | 4~20 个 tile，30~200 KB | 浏览器 HTTP cache | 两处：(1) MapLibre `raster-dem` hillshade 图层（默认关，`TerrainToggleControl`）；(2) `lib/viewshed/ElevationGrid.js:166` 直接 `fetch()` + `createImageBitmap` + `OffscreenCanvas` 解码 → `Float32Array` 灰度栅格 | 走廊分析记录 `missingTiles` / coverage gap；hillshade 图层空；已被 `traceFetch` 包 |
| `geocoder.bradr.dev` | **第三方**地理搜索：`/search?q=…&limit=6&autocomplete=true`（地名） + `/id/{gers}`（GERS ID 查询） | 每次输入一次，几 KB | 无 | 主线程 `lib/geocoder.js` + `components/nav/SearchBox.jsx` + `components/landing/CitySearch.jsx` | 搜索没结果，无错误弹窗 |
| `fonts.googleapis.com` + `fonts.gstatic.com` | Material Symbols Outlined（icon 字体，subset 指定）+ TikTok Sans 字体 | 20~80 KB woff2 | 浏览器 HTTP cache | `app/layout.jsx:44-51` `<link rel="stylesheet">` | UI 里图标变成豆腐块 |

### 2.2 自托管资源（`public/` 目录）

| 文件 | 用途 | 来源 |
|---|---|---|
| `maplibre-gl-worker.mjs` | MapLibre v6 强制要求自服务的 web worker | `postinstall` 从 `node_modules` 拷来（`scripts/copy-maplibre-assets.mjs`） |
| `maplibre-gl-shared.mjs` | MapLibre v6 shared module | 同上 |
| `mapbox-gl-rtl-text.js` | 阿拉伯语/希伯来语 shaping plugin | 同上 |
| `Noto Sans.woff2`、`Noto Sans Italic.woff2`、`PT Mono.woff2` | UI 字体（`app/layout.jsx:13-35` `@font-face`，`MapView.jsx:187` `document.fonts.load` 等待） | 手工放入 |
| `icons/*.svg` | 地图 POI 图标 | Maki/Overture 派生 |
| `favicon.png`、`darkmode.svg`、`lightmode.svg`、`download.svg`、`omf_logo_transparent.png` | UI 静态素材 | 手工 |
| `nybb.parquet` | NYC 5 区边界（历史遗留，用途待确认） | 手工 |

**重要注意事项**：MapLibre style **未设 `glyphs:` URL** —— 地图上的文字标签走浏览器已装的系统字体，不请求远端字体图集。这就是为什么 Overture Explorer 老代码里那套 glyph 服务在 VANTAGE 里不存在。

### 2.3 硬编码常量

| 常量 | 值 | 位置 |
|---|---|---|
| STAC_CATALOG_URL | `https://stac.overturemaps.org/catalog.json` | `lib/stacService.js:5` |
| TERRAIN_TILE_URL | `https://s3.amazonaws.com/elevation-tiles-prod/terrarium/{z}/{x}/{y}.png` | `lib/viewshed/ElevationGrid.js:22` |
| GEOCODER_BASE | `https://geocoder.bradr.dev` | `lib/geocoder.js:1` |
| ANALYSIS_RADIUS | 1500 m | `LaunchPointControl.jsx:34` |
| RADIAL_SPACING / ANGULAR_SPACING | 40 m / 6° | 同上 |
| CORRIDOR_BUFFER_METERS | 200 m | `lib/geo/corridorBuildings.js` |
| TERRAIN_TILE_ZOOM | 14 | `lib/viewshed/ElevationGrid.js:24` |
| BUILDING_TILE_ZOOM | (14, 见 corridorBuildings) | `lib/geo/corridorBuildings.js` |
| FIREWORKS_PRESETS | 12 个真烟花秀硬编码 | `lib/fireworksPresets.js` |
| BOOKMARKS | 4 个相机预设 | `components/BookmarkDial.jsx:12-41` |

---

## 三、运行时架构

VANTAGE 是 **静态导出**（`next.config.mjs`: `output: "export"`），没有 Next.js server runtime，任何 `page.jsx` 里写的东西最终都跑在浏览器。

### 3.1 主线程

装了几乎所有的东西：MapLibre GL、React、MUI、STAC 客户端、PMTiles + MVT + PBF 解码（`corridorBuildings.js` 里 dynamic import）、地形 tile 抓取 + PNG 解码、fetch trace、UI 全部。

**主线程重活儿的两个来源**：
1. 走廊建筑抓取 + 解码（`loadBuildingsAlongCorridor`）：一次几百个 tile 的 PBF 解码，1~4 MB 二进制。
2. 走廊地形抓取 + PNG → Float32 解码（`loadElevationGridForCorridor`）：几个 tile 但每个都要 `createImageBitmap` + Canvas readPixels。

### 3.2 MapLibre 自己的 Web Worker

MapLibre v6 强制要求。托管在 `public/maplibre-gl-worker.mjs`。做 tile parsing、feature indexing 等。**我们不控制内容**，是 MapLibre 上游代码。

### 3.3 VANTAGE 的 viewshed Worker

`lib/viewshed/worker.js`（40 行）。**唯一由我们写代码的 Worker**。

Imports 只有 3 个纯 JS 模块：
```
computeViewshed     -- lib/viewshed/computeViewshed.js
computeRooftopLayer -- lib/viewshed/computeRooftopLayer.js
ElevationGrid       -- lib/viewshed/ElevationGrid.js（这里只用其 class，不含 fetch/decode 逻辑）
```

**Worker 里没有 npm 依赖**。数据（buildings、terrain grid）由主线程 postMessage 送过去。

### 3.4 构建时 / 开发时

- `scripts/copy-maplibre-assets.mjs`（postinstall）：从 `node_modules` 拷 MapLibre worker + shared + RTL plugin 到 `public/`
- `scripts/dont-dream-its-overture.mjs`：Overture schema 验证 / tile spec 生成 CLI（`npm run overture` 系列）。**只是开发工具，不进 bundle**。
- `next.config.mjs`：`execSync("git rev-parse --short HEAD")` 把 SHA 注入到 `NEXT_PUBLIC_GIT_SHA` env

---

## 四、依赖清单

按角色分组，重点标注可能可迁移的部分。

### 4.1 dependencies（进生产 bundle）

| 包 | 角色 | 大小 | 跑在哪 |
|---|---|---|---|
| `maplibre-gl` ^6.4 | 地图渲染，自带 worker | **重**（~600 KB gz） | 主线程 + 自己的 worker |
| `pmtiles` ^4.5 | PMTiles archive reader（HTTP range 请求） | 小（~30 KB gz） | 主线程（MapLibre protocol handler + `corridorBuildings.js` dynamic import） |
| `@mapbox/vector-tile` ^3 | MVT PBF 解码 | 小 | 主线程，`corridorBuildings.js` dynamic import |
| `pbf` ^5.1 | Protobuf reader（`@mapbox/vector-tile` 的 peer） | 小 | 主线程，`corridorBuildings.js` dynamic import |
| `stac-js` ^0.5 | STAC 目录解析 | 小-中，纯 ESM | 主线程（`lib/stacService.js`） |
| `@geoarrow/geoarrow-wasm` 0.2 | GeoArrow → GeoJSON WASM 解码 | **重**（WASM） | 主线程，仅 `lib/normalizeGeojson.js` 引用 |
| `fflate` ^0.8 | gzip 解压（PMTiles header 用） | 小 | 主线程（间接） |
| `@mapbox/mapbox-gl-rtl-text` | RTL 文字 shaping plugin | 小 | 独立脚本从 `public/` 加载 |
| `@mapbox-controls/language` / `tooltip` | MapLibre 控件 | 小 | 主线程 |
| `@maplibre/maplibre-gl-inspect` | inspect overlay | 中 | 主线程（flag 关时不激活） |
| `@mui/material` / `@mui/icons-material` / `@mui/x-tree-view` / `@emotion/react` / `@emotion/styled` | UI | **重** | 主线程 |
| `iconoir-react` | 图标（Bug 图标等） | 小（tree-shake） | 主线程 |
| `react` / `react-dom` ^19 / `next` ^16 / `prop-types` | 框架 | **重** | 主线程 + 构建时 |

### 4.2 devDependencies（不进 bundle）

`@axe-core/playwright`、`eslint`、`jest`、`playwright` 等常规构建/测试栈。

---

## 五、可视度分析 pipeline 数据流

从"用户放 launch point"到"看到 dock 里的 profile 和 preview"完整走一遍。

### 5.1 圆内网格分析（launch pin 落地后自动跑）

```
放 launch point (LaunchPointControl click handler)
   ↓
[主线程] HiddenBuildingSource.query(launch, 1500m)  → 拿 1500m 圆内所有建筑
[主线程] loadElevationGridForBounds(bbox)           → fetch 4~9 张地形 tile + PNG decode
   ↓
[worker] postMessage({buildings, terrainGrid, launch, caliber, ...})
   ↓
[worker] computeViewshed()   → radial sweep, 60 方位角 × 37 环 = 2220 格
[worker] computeRooftopLayer() → 每栋楼一个 fraction
   ↓
[主线程] postMessage → grid + rooftop → setData 两个 MapLibre 源
   ↓
[主线程] pickTopSpots(grid) → 显示 3 个 top-spot 数字标记
```

**耗时**（SF density）：query ~1.5 s，compute ~2 s（Radial Sweep 优化后），总 ~4 s。

### 5.2 走廊分析（用户点观察点后自动跑）

**用哪套取决于距离**：

- **观察点在 1500m 圆内**：复用 `HiddenBuildingSource.query` 已经拿到的建筑（无网络请求）。
- **观察点在圆外**（"任意位置观察点"路径）：
  ```
  loadBuildingsAlongCorridor({from, to, buffer=200m})
    ↓
  tilesAlongCorridor(from, to, z=14, buffer=200m) → 决定要哪些 tile（几 ~几十个）
    ↓
  for each tile:
    PMTiles.getZxy(z,x,y) → HTTP range request → gzip PBF bytes
    new VectorTile(new Pbf(bytes)) → 解出 building + building_part 层
    → 记 coverageGap 如果 tile 不存在
    ↓
  loadElevationGridForCorridor({from, to, buffer=200m})
    → fetch 每个 Terrarium tile → createImageBitmap → OffscreenCanvas readPixels → Float32Array
    ↓
  computeSightlineProfile({observer, launch, buildings, terrainGrid, ...})
    → 主线程一次直射线求交，非 worker
    → 返回 hits[] + terrainProfile[] + verdict
  ```

**这段是主线程最重的一处**：PBF 解码是 CPU-bound，UI 卡感来源。

### 5.3 剖面 / 3D preview

`computeSightlineProfile` 的输出直接喂 `SkylineChart` 和 `PerspectivePreview`。**渲染纯 SVG，无额外数据**。

### 5.4 曲率修正

`lib/viewshed/curvature.js` 的 `apparentAltitude`：地球曲率 + 大气折射常数 `k=1.13`。距离 >2000m 时启用。纯数学，无 I/O。

---

## 六、Cloudflare Workers 迁移候选分析

**不做推荐**，只列每个候选的：能做什么、需要什么、tradeoffs。

### 6.1 强候选（现在缺、迁过去就有）

#### 候选 1：Report-a-problem 收集端点

- **现状**：`lib/debug/bundle.js:164` `sendReport` 只支持 clipboard/download。注释里写着 "future email / server POST"。
- **搬什么**：接一个 `POST /report` 到 CF Worker，把 bundle 存 R2/D1/KV，或转发到你的邮箱/Slack/Linear。
- **代价**：小。前端只需要 `sendReport` 加一个 `mode: "post"` 分支，加一个环境变量指向 Worker URL。
- **额外好处**：可以做 rate limiting、去掉重复上报、把 build ID + repro URL 做成一个索引。
- **注意**：bundle 里带 lat/lng（隐私），Worker 端可以选择做坐标粗化 / 只存哈希。

#### 候选 2：地理搜索代理（干掉对 `bradr.dev` 的依赖）

- **现状**：`lib/geocoder.js`、`SearchBox`、`CitySearch` 都直接打 `https://geocoder.bradr.dev`。这是**第三方**主机，你不控制它的 SLA、rate limit、schema 变动。
- **搬什么**：CF Worker `GET /geocode?q=…` 转发到 Nominatim（免费）/ Photon / Pelias / Overture 自有 GERS index，返回统一 shape。
- **代价**：中等。前端需要改一个 URL；Worker 需要处理三种搜索模式（Locality/Country/GERS）；Overture GERS lookup 可能需要 `Overture Places` PMTiles + spatial index，工作量最大。
- **额外好处**：搜索延迟更低（CF 全球边缘）；能加缓存。

### 6.2 中等候选（能显著减重，但要搬走一些代码）

#### 候选 3：STAC catalog + PMTiles URL 索引

- **现状**：冷启每次要打 1(catalog) + 1(release) + 6(theme) = 8 次网络请求解析出 6 个 PMTiles URL。热启命中 localStorage 只打 1 次。见 `lib/stacService.js:103-159`。
- **搬什么**：CF Worker cron 每天 pin 一次最新 release，把结果存 KV。前端只打一次 `GET /stac/current` 拿 `{releaseId, urls: {base, buildings, ...}}`。
- **代价**：小。前端 `loadStacData` 换成一次 fetch。Worker 端 10 行代码 + KV bucket。
- **额外好处**：明确的版本 pin 能力（现在 URL 里有 `stac=<releaseId>` 但没真的用它 rebind），bundle 里报的 `stacRelease` 变成真的可复现。可以给不同用户/环境 pin 到不同 release 做灰度。

#### 候选 4：走廊建筑抓取 + 解码

- **现状**：`lib/geo/corridorBuildings.js` 在**主线程** dynamic import `pmtiles` + `@mapbox/vector-tile` + `pbf`，做 PBF 解码。这是主线程最重的一处 CPU 负担（PBF 解码是 UI 卡顿主因）。
- **搬什么**：CF Worker `POST /buildings/corridor` 接 `{from, to, buffer, releaseId}`，返回已经解码好的 GeoJSON 建筑（可能压缩过）。
- **代价**：**大**。Worker 需要能 fetch PMTiles + 有 PBF/MVT 解码能力（`@mapbox/vector-tile` + `pbf` 在 Cloudflare Workers runtime 应该能跑，需验证）。Worker 每请求带宽比现在主线程范围请求更大（因为要传解出来的 GeoJSON 而不是原 PBF）。
- **好处**：干掉 `pmtiles` + `@mapbox/vector-tile` + `pbf` 三个 npm 依赖不进 browser bundle（每个都不大，但加起来 30~80 KB + WASM/complexity）；主线程 UI 更顺；返回结果可以是精简过的（只留 corridor 相交的那些 footprint），网络传输量可能反而更小；能加 Worker 端缓存（同 corridor 多次访问）。
- **注意**：`HiddenBuildingSource` 里那套 MapLibre 二实例查询还是要保留（用于 1500m 圆内的分析），因为它复用了 MapLibre 已加载的瓦片缓存。所以 corridor 是纯"圆外那 20km 距离长条"的 case。

#### 候选 5：地形 Elevation Grid

- **现状**：`lib/viewshed/ElevationGrid.js` 主线程 `fetch` PNG tile → `createImageBitmap` → `OffscreenCanvas` readPixels → Terrarium 解码 → 拼一个 `Float32Array` 灰度栅格。~30~200 KB PNG × 几张。
- **搬什么**：CF Worker `POST /elevation/bbox` 接 bbox，返回一个二进制 grid（Float32Array 直接序列化）。
- **代价**：中。Worker 端需要能解 PNG + 拼栅格。CF Workers 有 `Image` API 的话直接用，没有就用 upng-js 之类的纯 JS 库。
- **好处**：干掉 `createImageBitmap` + Canvas 依赖；主线程更轻；Worker 端可以缓存热点 bbox。
- **注意**：走廊场景一次几张 tile 不太多，节约度有限。真正划算是 launch pin 1500m 圆的 bbox 那次——那是每次落 launch pin 必跑的。

### 6.3 弱候选（收益不明显 / 反而变差）

#### 候选 6：computeViewshed 本身搬到 CF Worker

- **不建议**。理由：
  - 每次分析是 per-user 数据（用户挑的 launch point，跟其他用户不相关，没有缓存价值）
  - 结果 payload 大（2220 个 GeoJSON feature + 一个 rooftop 层）
  - 已经在浏览器 Worker 里跑，不阻塞 UI
  - 输入数据（buildings + terrain grid）要序列化传到 CF Worker，往返延迟很可能 > 主线程 2s 计算时间

**只有一种情况值得考虑**：如果你以后要做"预计算 top spots"（比如整个 NYC 每平方公里的最佳观景点缓存起来），那 CF Worker + Durable Objects/R2 才有价值。这是产品功能扩展不是性能优化。

### 6.4 不适合搬 / 已经在正确位置

- **静态站点托管本身**：直接用 Cloudflare Pages 托管 `out/` 目录，是最正确的做法，不需要专门讨论。
- **MapLibre 内部 tile fetch**：MapLibre 上游代码，不能拦。
- **Overture PMTiles 本身**：Overture 已经在 S3 服务，我们不需要 rehost。真要控制访问的话可以在 CF Worker 里做 proxy + 缓存层，但这更像"数据自主可控"的战略决策而不是性能优化。

### 6.5 快速看板

| 候选 | 是否现在有 | 迁移难度 | 主要好处 |
|---|---|---|---|
| 报告收集端点 | 无 | 极小 | 让 report 真的能收 |
| 地理搜索代理 | 有（第三方） | 中 | 去第三方依赖 |
| STAC 目录/PMTiles 索引 | 有 | 极小 | 8 次网络 → 1 次 + 真 pin release |
| 走廊建筑抓取+解码 | 有 | 大 | 主线程去掉 PBF/MVT/pmtiles 依赖，UI 更顺 |
| 地形 elevation grid | 有 | 中 | 主线程去掉 PNG decode，Worker 侧可缓存 |
| computeViewshed | 有（browser worker） | / | 不建议搬 |
| 静态站点托管 | 有（out/ 目录） | 无 | 直接放 CF Pages |

---

## 附：跟 Overture 世界的关系速图

```
                                                      ┌──────────────────┐
                                                      │  你的浏览器      │
                                                      └────────┬─────────┘
                                                               │
                                       ┌───────────────────────┼───────────────────────┐
                                       │                       │                       │
                                       ▼                       ▼                       ▼
                       ┌───────────────────────┐  ┌──────────────────────┐  ┌────────────────────┐
                       │ Overture STAC catalog │  │  Overture PMTiles S3 │  │  AWS Terrain Tiles │
                       │ stac.overturemaps.org │  │ (release-specific)   │  │ Terrarium PNG      │
                       └───────────────────────┘  └──────────────────────┘  └────────────────────┘

                                                                            ┌────────────────────┐
                                                                            │ geocoder.bradr.dev │
                                                                            │ (第三方，非你控制)  │
                                                                            └────────────────────┘

                                                                            ┌────────────────────┐
                                                                            │  Google Fonts      │
                                                                            └────────────────────┘
```

上线后你可以做的最"名字自主"的一步：把 `bradr.dev` 换成自己的 CF Worker（候选 2）。次自主的一步：把 STAC 8 次请求收成 1 次（候选 3）。真想跟 PMTiles 前端 decode 做告别：候选 4。
