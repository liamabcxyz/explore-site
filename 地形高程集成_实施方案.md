# 地形高程集成 — 实施方案

**项目**：VANTAGE 烟花视觉分析工具
**基线技术栈**：Next.js (App Router, static export) · MapLibre GL JS · PMTiles (STAC catalog) · Overture Maps data · MUI · Web Worker for viewshed compute
**问题**：当前视线数学假设地面绝对平坦 z=0，忽略地形起伏
**状态**：可执行实施方案（此文替代并扩展原设计文档 `地形高程集成_工程设计文档.md` — 该原文档由用户维护于本仓库外，仅记录设计推导过程；此实施方案自成一体，实施时无需回看）

**本文档 vs. 原设计文档**：原设计文档（`地形高程集成_工程设计文档.md`，用户维护的外部参考文档，非本仓库文件）从零推导出架构方向；本文档保留其核心决策（AWS Terrain Tiles、Terrarium 编码、2D 显示 + 绝对海拔计算的分离、跟径向扫描的协同实现），补充了三块原文档未覆盖的关键内容——**跟当前代码库的集成清单**、**测试迁移策略**、**实现级设计决策**——并把估工从"6-11 天"修正为"2-3 周实际工作量"。此实施方案自成一体，无需回看原文档即可执行。

---

## 目录

1. [背景与动机](#1-背景与动机)
2. [数据方案](#2-数据方案)
3. [展示层设计](#3-展示层设计)
4. [计算层重构](#4-计算层重构)
5. [与当前代码库的集成清单](#5-与当前代码库的集成清单)
6. [测试迁移策略](#6-测试迁移策略)
7. [分阶段实施](#7-分阶段实施)
8. [实现级设计决策](#8-实现级设计决策)
9. [风险与降级](#9-风险与降级)
10. [依赖关系与后续项](#10-依赖关系与后续项)

---

## 1. 背景与动机

### 1.1 问题

当前 `lib/viewshed/sightline.js` 的所有高度计算基于统一的地面基线（观察者 1.6m、建筑 `height`、烟花球 `H`），隐含"地面处处齐平"的假设。在地形起伏显著的城市（SF 旧金山，Twin Peaks 280m；西雅图；香港；东京部分区域），该假设导致两类系统性错误：

| 错误类型 | 表现 | 后果 |
|---|---|---|
| **假阴性** | 山顶/坡地实际海拔高于周边建筑，视线可越过阻挡，但算法判为不可见 | 漏掉最优观赛点（Twin Peaks、Bernal Heights 等） |
| **假阳性** | 低洼处无建筑遮挡但被山体阻挡，算法判为可见 | 推荐用户前往实际看不到的位置（`pickTopSpots` 会把这种点标为 #1/#2/#3） |

在目标城市中，**地形落差量级远超绝大多数建筑高度**（SF 300m 山头 vs. 典型建筑 20-60m），因此地形是比建筑更强的决定因素。

### 1.2 数据缺口

Overture Maps 提供 buildings（含 `height`）、places、transportation、base 等 theme，**不包含地形高程数据**。高程必须从外部引入并在分析层面与 Overture 数据对齐。

### 1.3 已完成的诚实披露

当前 UI 层已经在两处（`components/launch/LaunchPointControl.jsx` 面板 + `components/analysis/ProfilePanel.jsx`）显示斜体免责声明：

> "Analysis considers buildings only — trees, terrain, and weather aren't factored in."

**本方案实施完成后需要将 "terrain" 从声明中删除**（详见 §5.4）。

### 1.4 差别化价值

同类工具多数只考虑建筑遮挡。正确纳入地形后，推荐结果将与"只看建筑"的工具产生显著且更准确的差异，是产品的核心技术壁垒。

---

## 2. 数据方案

### 2.1 数据格式：Terrain-RGB 栅格瓦片

高程数据在 Web 地图生态中的标准形式是把海拔编码进 PNG 的 RGB 通道。两种主流编码：

**Terrarium**（AWS Terrain Tiles）：
```
height = (R * 256 + G + B / 256) - 32768
```

**Mapbox Terrain-RGB**：
```
height = -10000 + ((R * 65536 + G * 256 + B) * 0.1)
```

### 2.2 数据源选型

| 来源 | 精度 | 成本 | 评估 |
|---|---|---|---|
| **AWS Terrain Tiles** (`elevation-tiles-prod`) | 美国境内基于 NED，约 10m | 免费（AWS 开放数据） | **第一阶段选用**：Terrarium 编码，开箱即用，`maplibre-gl` 原生 `raster-dem` source 支持 |
| 自建 USGS 3DEP 分片 | 1m / 10m | 数据免费，需自行处理与托管 | 精度更高（美国境内），用 GDAL 分片切片为 PMTiles，与现有托管线一致 |
| MapTiler / Mapbox terrain | 10-30m | 有限免费额度 | 类似，但引入外部账号依赖 |

**决策**：第一阶段使用 **AWS Terrain Tiles**。地形是连续场，10m 分辨率已充分（区别于建筑轮廓需米级精度）。若后续在陡峭区域发现精度不足，再迁移至自建 3DEP PMTiles——托管方式无需改动。

### 2.3 数据量评估

分析范围为发射点周边 1.5km 半径（`ANALYSIS_RADIUS` in `components/launch/LaunchPointControl.jsx:26`）：

```
300 × 300 网格 @ 10m 分辨率 = 90,000 个 float32 ≈ 360 KB
```

可完全常驻内存，无需分页或流式加载。**每次发射点变化时重取**（跟建筑数据同样节奏）。

### 2.4 归属署名要求（原文档遗漏）

AWS Terrain Tiles 派生自 Mapzen 的混合数据（USGS NED、SRTM、Copernicus 等），使用时需在地图上添加归属字符串。示例：

```
"Terrain data © USGS, SRTM, Mapzen — via AWS Open Data"
```

添加位置：`components/MapView.jsx` 的 attribution control 里，或者跟已有的 `© OpenStreetMap contributors · Overture Maps Foundation` 归属并列。实施于第 1 阶段（跟视觉图层同一 PR）。

---

## 3. 展示层设计

MapLibre GL JS 原生支持地形数据源，展示层改动极小。

### 3.1 注册 raster-dem source

在 `MAP_STYLE`（`components/MapView.jsx:68` 附近）或 `lib/LayerManager.js` 的 `addSources` 中新增（跟现有 6 个 PMTiles source 并列）：

```js
{
  sources: {
    "aws-terrain": {
      type: "raster-dem",
      tiles: ["https://s3.amazonaws.com/elevation-tiles-prod/terrarium/{z}/{x}/{y}.png"],
      encoding: "terrarium",
      tileSize: 256,
      maxzoom: 15,
      attribution: "Terrain © USGS/SRTM/Mapzen via AWS"
    }
  }
}
```

### 3.2 渲染方案：Hillshade（选定方案）

新增 hillshade 图层，**地图保留俯视 2D 视角**，通过明暗阴影表达地形起伏：

```js
{
  id: "hillshade",
  type: "hillshade",
  source: "aws-terrain",
  paint: { "hillshade-exaggeration": 0.5 }
}
```

**选定理由**：
- 符合既定的"计算 3D、展示 2D"产品方向
- 渲染与可视化色块**不冲突**，与阻挡核心分析结果并存不打架
- 移动端开销低，契合"活动当天户外临场查询"的使用场景

图层顺序：注册在建筑挤出图层之下、基础地面色块之上（`components/map/layers/explore/base/land-cover/*.json` 之上，`components/map/layers/explore/buildings/*/extrusion.json` 之下）。

### 3.3 备选方案（暂不实施）

| 方案 | 说明 | 决策 |
|---|---|---|
| 等高线 | 额外 contour 瓦片，或用 `maplibre-contour` 从 terrain-RGB 实时生成 | 待定，视用户是否需要精确高度读数 |
| `map.setTerrain()` 的 3D 地形 | MapLibre 原生支持隆起地形 | **不实施**：建筑挤出会跟地形穿模；增加移动端负担。可保留为将来可选开关 |

### 3.4 约束：不得使用 `queryTerrainElevation` 做计算

MapLibre 提供 `map.queryTerrainElevation(lngLat)`，但**不适用于本项目的计算路径**：

- 仅在 `setTerrain()` 启用后生效（而我们不启用 3D 地形）
- 依赖当前视口已加载的瓦片，视口外区域返回 null（跟 `HiddenBuildingSource` 之前解决的建筑视口裁剪 bug 是同一类问题）
- 运行于主线程，Worker 无法调用
- 高频调用数万次性能不可接受

**结论**：展示层与计算层各自独立获取地形数据。

---

## 4. 计算层重构

### 4.1 数据获取管线

```
用户设定发射点
  ↓
计算覆盖 1.5km 半径所需的 terrain 瓦片列表 (z/x/y)   [通常 4-9 张]
  ↓
fetch PNG（在 Web Worker 内完成，详见 §8.1）
  ↓
createImageBitmap / OffscreenCanvas 解码像素
  ↓
按编码公式转换为 Float32Array 高程网格
  ↓
以 transferable 传入 viewshed Worker
  ↓
Worker 内计算全程本地查询，零网络、零主线程通信
```

### 4.2 新增模块：`lib/viewshed/ElevationGrid.js`

```js
class ElevationGrid {
  // 内部状态：Float32Array + 原点经纬度 + 每格米数 + 数据源标签
  //（预留基准面标签，见 §9.3）

  getElevation(lng, lat)    // 双线性插值，返回米
  getElevationLocal(x, y)   // 本地平面坐标版本，热路径使用
  hasCoverage(lng, lat)     // 是否落在已加载范围内（边缘处理，见 §8.3）
}
```

**双线性插值为必需项**——不可直接取最近格式，否则坡地上会产生台阶状伪影，污染可视化边界的平滑度。

该模块隔离单一职责，无外部依赖，易于单元测试。

### 4.3 改造：统一到海拔绝对基准

`computeViewshed.js`、`computeRooftopLayer.js`、`computeProfile.js` 中的所有高度由"相对局部地面"改为"绝对海拔"：

| 原实现 | 改造后 |
|---|---|
| 观察者高度 = `1.6` | `grid.getElevation(点) + 1.6` |
| 楼顶高度 = `building.height` | `grid.getElevation(建筑质心) + building.height` |
| 靶点高度 = `H` | `grid.getElevation(发射点) + H`（H 从 `deriveShellParams(caliber).targetHeight` 得到） |

**遮挡判断逻辑本身无需修改**——它是比较仰角 / 天际线角，从输入即变为在同一海拔基准下的绝对值。

### 4.4 新增：地形作为遮挡物

山脊、坡地本身会阻挡视线，不仅仅建筑会。**此项必须实现**，否则地形集成不完整（这也是原设计文档明确否决"只对端点做高程修正"这种半吊子方案的原因——那种做法修好了"我的点在山上"，却留着"中间隔着山"这个杀手场景仍然错，反而让用户信任度上升到不该有的水平）。

天然嵌入**径向扫描（Radial Sweep）方案**（原 `可视域计算优化方案.md` 方案 B，此前推迟）的实现：

```
对每个方位角，沿射线从内向外，每步 10-20m：
  1. 采样该位置地形海拔                    [新增]
  2. 计算地形对靶点的阻挡角                [新增]
  3. 检查该位置建筑，计算楼顶阻挡角
  4. 取两者中更强者，更新"天际线运行最大值"
  5. 后续任何点上述运行最大值 → 遮挡判定
```

**这是优先实施径向扫描的合并理由**：地形是连续场，在"逐点独立遮挡查询"的旧架构下要么被迫离散化为块状遮挡物（实现别扭），而径向扫描本就沿射线行进，采样地形属顺路操作，边际成本接近零。

### 4.5 与方案 A（建筑独立叠层）的交互

建筑叠层计算时（`lib/viewshed/computeRooftopLayer.js`），**同一建筑的地基海拔须取单一值**（建议取轮廓质心处地形高度），不可令建筑各角落使用不同基准，否则倾斜建筑会产生楼顶不水平的错误占位。目标城市中一栋建筑横跨 5-10m 高差属常见情形，此项不可忽略。

---

## 5. 与当前代码库的集成清单

**（本节为原设计文档未覆盖的关键内容）**

本 session 期间新增了多个基础设施模块，地形集成必须与之协调。以下清单按依赖顺序列出：

### 5.1 `lib/HiddenBuildingSource.js` — 隐藏第二地图（已实装）

**背景**：`map.querySourceFeatures()` 只能查询用户当前视口已加载的瓦片；早期版本导致 1.5km 分析范围被视口硬截断（"rooftop view 呈方形而非圆形"）。修复方案是隐藏第二个 maplibre 实例，专门为 1.5km 范围加载建筑数据。

**跟地形的集成决策**：
- **选项 A**：让 `HiddenBuildingSource` 同时管理 buildings + terrain 两种数据。改动小，共用 fetch/生命周期。
- **选项 B**：新增独立的 `lib/HiddenTerrainSource.js`。职责分离更清晰，但复制了整套 tile 加载/缓存逻辑。

**推荐 A**：改名为 `HiddenAnalysisSource`（或保留原名但语义扩展），在同一实例中并注册 `aws-terrain` source 和 buildings 两种 source。加载完成的 `idle` 事件天然对齐两种数据。

**待改文件**：`lib/HiddenBuildingSource.js`（重构或改名），`components/launch/LaunchPointControl.jsx`（消费方，把 `getHiddenSource` 的返回值扩展为同时提供建筑和地形网格）。

### 5.2 `lib/geo/rooftopBase.js` — 楼顶发射基准（已实装）

`findRooftopBase(launch, buildings)` 目前返回"发射点所在建筑的 `height`"，作为 `targetHeight = deriveShellParams(caliber).targetHeight + rooftopBase` 的加数（`LaunchPointControl.jsx:66`）。

**改造**：加上地形后应为
```
launchAbsoluteAltitude = grid.getElevation(launch) + rooftopBase + caliberHeight
```
即 `findRooftopBase` 返回值语义不变（仍是"建筑本身贡献的高度"），但**调用方**需把它跟 `grid.getElevation(launch)` 相加，最终传递给 Worker 的 `targetHeight` 是**绝对海拔**。

**待改文件**：`components/launch/LaunchPointControl.jsx`（grid effect 和 profile effect 内的 worker.postMessage 参数构造）。

### 5.3 `viewerLevel`（ground/floor/rooftop）— 观察者高度选择（已实装）

`components/analysis/ProfilePanel.jsx` 的 ToggleButtonGroup 允许用户选择观察者位于所在建筑的地面/某层/屋顶。当前实现（`LaunchPointControl.jsx:344-350`）：

```js
let observerHeight = EYE_HEIGHT;
if (building && viewerLevel.mode === "rooftop") {
  observerHeight = building.height + EYE_HEIGHT;
} else if (building && viewerLevel.mode === "floor") {
  observerHeight = (floor - 1) * METERS_PER_FLOOR + EYE_HEIGHT;
}
```

**改造**：`observerHeight` 也变为绝对海拔：
```js
const groundElev = grid.getElevation(observer.lng, observer.lat);
let observerAbsAlt = groundElev + EYE_HEIGHT;
if (building && viewerLevel.mode === "rooftop") {
  observerAbsAlt = groundElev + building.height + EYE_HEIGHT;
}
// ... 类似
```

**待改文件**：`components/launch/LaunchPointControl.jsx` 的 profile effect（约 line 383+，`computeSightlineProfile` 调用前）。

### 5.4 已落地的免责声明 — 需部分回收

两处显示的免责声明：
- `components/launch/LaunchPointControl.jsx`（约 line 847）
- `components/analysis/ProfilePanel.jsx`（约 line 274）

内容：`"Analysis considers buildings only — trees, terrain, and weather aren't factored in."`

**改造**：地形集成完成后，两处文案调整为：
```
"Analysis considers buildings and terrain — trees and weather aren't factored in."
```

作为最后阶段（§7 阶段 5）的验收动作。

### 5.5 `lib/viewshed/computeProfile.js` — 单点剖面路径

`ProfilePanel` 的 SVG 剖面图走**独立**计算路径 `computeSightlineProfile`（不复用 grid 计算）。此路径也需要地形数据：观察者→发射点连线的地形沿程 profile，恰好是 SVG 剖面图渲染的自然数据源。

**改造**：`computeSightlineProfile` 增加 `terrainGrid` 参数，沿观察者→发射点采样地形，输出扩展为：
```js
{
  totalDistance, minAlt, frac, category, ...,
  hits,              // 建筑遮挡命中（已有）
  terrainProfile,    // [新增] 沿程地形海拔序列 [{distance, elevation}]
}
```

**渲染改造**：`components/analysis/ProfilePanel.jsx` 的 `SightlineChart` 组件新增一条"地形填充曲线"图元（如浅棕色多边形），压在建筑柱之下、视线之上。这才是原设计文档 §5 说的"剖面图作为副产物"的真正实现位置——不是 grid 层的横切图（那个不存在），而是 ProfilePanel 已有的 SVG。

**待改文件**：`lib/viewshed/computeProfile.js`（返回值扩展），`components/analysis/ProfilePanel.jsx` 的 `SightlineChart` 内 SVG 渲染部分。

### 5.6 `lib/viewshed/pickTopSpots.js` — 推荐前 3 观影点（已实装）

不需要直接改动，但需要**在集成后重新验证**：现在的 Top 3 是纯粹按 `score` 排序，而 `score` 依赖 `frac`（可见性）和 `comfort`（观赛角）。地形集成后 `frac` 会变（Twin Peaks 变绿、洼地变红），Top 3 会自然更换，无需修改算法。作为**验收信号**——观察 Top 3 在 SF Financial District 发射点下从"周边平地"迁移到"周边高地"是关键正确性判据（§7 阶段 3）。

### 5.7 `lib/viewshed/buildingIndex.js` — 空间索引（已实装）

空间索引的 grid-cell 遍历是按 `queryBuildingIndex(observer, target)` 的**线段**查询。切换到径向扫描后（§4.4），扫描器接管每方位角一次的候选建筑集合收集，`queryBuildingIndex` 的调用模式从"每格一次"变为"每方位一次"。索引本身无需改动，但**调用方（`computeViewshed.js` 的循环嵌套结构）需要重构**成方位角外层、径向内层（详见 §7 阶段 4）。

### 5.8 Worker 通信协议

`lib/viewshed/worker.js` 目前接收 `{ launch, targetHeight, shellRadius, analysisRadius, radialSpacing, angularSpacing, buildings }`。地形集成后新增：

```js
{
  ...(已有字段),
  buildings,
  terrainGrid: {          // [新增] transferable
    buffer,               // ArrayBuffer of Float32Array
    originLat, originLng,
    metersPerCellX, metersPerCellY,
    cellsX, cellsY,
    verticalDatum: "EGM96",  // §9.3
  },
}
```

在 Worker 内重建 `ElevationGrid` 实例即可。**transferable 传递**避免复制大数组（`postMessage(msg, [terrainGrid.buffer])`）。

---

## 6. 测试迁移策略

**（本节为原设计文档未覆盖的关键内容）**

### 6.1 影响面

当前测试套件（`npm test`）：**25 个 suite，702 个用例**。以下测试文件的 fixture 假设"建筑高度即绝对海拔"：

| 文件 | 用例数 | 主要断言 |
|---|---|---|
| `__tests__/sightline.test.js` | 若干 | 手推 `req` 值、`tEntry`、`tExit` |
| `__tests__/computeViewshed.test.js` | 4 | 按 `frac`/`category` 断言每 cell |
| `__tests__/computeRooftopLayer.test.js` | 6 | 按 `frac`/`category` 断言每建筑 |
| `__tests__/computeProfile.test.js` | 若干 | 断言 `hits` 列表和 `frac` |
| `__tests__/pickTopSpots.test.js` | 6 | 依赖 `score` 值 |
| `__tests__/buildingIndex.test.js` | 若干 | 断言候选集合正确性 |

统一到绝对海拔后，所有 fixture 需要传入 `terrainGrid`（在测试中构造一个平地网格 `terrain(x,y) = 0` 保持向后兼容），或者传入非零地形网格测试新增能力。

### 6.2 迁移策略选项

**选项 A：并行 API（不推荐）**：新增 `computeViewshedWithTerrain(...)` 与旧 `computeViewshed(...)` 并存，逐步迁移调用方。
- 优点：任意时刻回归风险低。
- 缺点：两套实现长期共存需同步维护；不迁移的调用方永远错。

**选项 B：Big Bang，一次性迁移（推荐）**：所有 fixture 传入"平地地形网格"（`ElevationGrid.flat()`）保持数值一致；新增测试覆盖非零地形能力；旧 API 直接改签名。
- 优点：不留技术债；语义清晰。
- 缺点：需在同一 PR 中改所有 fixture。

**推荐 B**。理由：viewshed 算法是核心库，两套并行的维护成本比一次性迁移的短期风险高。此外，`ElevationGrid.flat()` 作为测试辅助函数，能保证平地情况下**数值精确等价**（不是"近似等价"），避免"迁移后 5 个用例误差在小数点第 6 位"这类噪声。

### 6.3 新增测试覆盖

除了保持旧用例通过外，新增专门测试地形能力：

- **单元测试** `__tests__/ElevationGrid.test.js`：双线性插值正确、边界外查询 `hasCoverage=false`、跨瓦片边界正确处理
- **集成测试** `__tests__/computeViewshedTerrain.test.js`：
  - 平地：结果与不带地形的旧 fixture **数值精确一致**（回归保障）
  - 山头挡住的低洼靶点：判为 blocked（新能力）
  - 观察者在山顶：判为 visible，即使中间有一栋 100m 建筑（Twin Peaks 场景）
- **快照** `computeSightlineProfile` 输出扩展 `terrainProfile` 字段的用例
- **回归** `pickTopSpots` 在有山地形下选出高地点位（Twin Peaks 场景的验收信号）

### 6.4 CI 验证顺序

阶段 3 完成时（详见 §7），跑 `npm test` 后应有：
1. 所有原有用例 **passing**（`ElevationGrid.flat()` 保障数值等价）
2. 新增地形用例 **passing**
3. 测试总数 ≥ 现有 702 + 新增

若阶段 3 完成时旧用例出现 flake 或数值偏移超过 1e-6，说明 `ElevationGrid.flat()` 逻辑或高度累加顺序错了——**立即回滚，不进入阶段 4**。

---

## 7. 分阶段实施

**（原文档 §6 的基础上，估工修正为实际工作量，每阶段依赖项显式列出）**

| 阶段 | 任务 | 估工 | 依赖 | 验收标准 |
|---|---|---|---|---|
| **1** | 展示层：AWS Terrain Tiles 注册 + hillshade 图层 + attribution | 0.5-1 天 | — | 地图上可见地形起伏阴影，色块未被遮挡 |
| **2** | `ElevationGrid` 模块（拉取 / 解码 / 双线性插值）+ 单测 | 2-3 天 | — | 已知点海拔正确（跨 4 张瓦片验证边界），插值无阶跃 |
| **3** | Viewshed 计算改绝对海拔基准（**不含地形阻挡**） | 2-3 天 | §5.1, §5.2, §5.3, §5.5, §6 | 高地点位可见性结果发生预期变化；`ElevationGrid.flat()` 保障旧测试全过 |
| **4** | 径向扫描实施 + 地形阻挡纳入 | 5-8 天 | 阶段 3 | Twin Peaks 场景：山体背后洼地正确判为不可见；SF 顶级观景台从红变绿 |
| **5** | 剖面图渲染加地形填充 + 免责声明文案修改 | 1-2 天 | §5.5, §5.4 | ProfilePanel SVG 中可见浅棕地形曲线；面板文案更新 |

**合计**：约 **10-17 个工作日**（含单元测试、集成测试、代码 review、可视化验收；相比原文档估工 6-11 天更贴近实际）。

**关键决策节点**：**阶段 3 完成后立即可获得正确性信号**——SF Financial District 发射点下，Twin Peaks 那一片高地本应从"被建筑遮挡的红色"转为"通视的绿色"。该可视化对比可作为判据决定是否继续阶段 4。若阶段 3 结果不符预期，先排查 §8 的坐标系对齐或垂直基准问题。

**可暂停节点**：**阶段 1 + 2 单独部署价值明确**（地图看起来 3D、用户能看见地形），不影响任何现有分析。若时间预算紧，先合并阶段 1-2，稍后再做阶段 3-5。

---

## 8. 实现级设计决策

**（本节为原设计文档未覆盖的关键内容）**

### 8.1 PNG 解码位置：Worker 内部

**决策**：所有地形瓦片的 fetch + PNG 解码 + 数值转换全部在 Web Worker 内完成，不在主线程做。

**理由**：
- 主线程解码 4-9 张 PNG（每张 ~30KB）可能导致 100-300ms 的 blocking，用户拖动时明显 jank
- 现有的 viewshed Worker（`lib/viewshed/worker.js`）已有独立执行环境；扩展它比新开 Worker 简单
- Worker 内 `createImageBitmap(await (await fetch(url)).blob())` 已在所有目标浏览器可用（Chrome ≥54、Firefox ≥79、Safari ≥15）
- 解码后的 Float32Array 通过 transferable 零复制送回主线程或直接留在 Worker

**实施要点**：`lib/viewshed/worker.js` 的 `self.onmessage` 处理器扩展，先处理 terrain 消息（返回 grid），再复用 grid 处理 viewshed 消息。或提供两个消息类型：`{ type: "loadTerrain" }` 和 `{ type: "computeViewshed" }`。

### 8.2 坐标系对齐：局部等距 vs. Web Mercator

**背景**：
- 地形瓦片按 Web Mercator（EPSG:3857）分片，格点均匀分布在 Mercator 坐标下
- 现有代码用 `lib/geo/toLocalMeters.js` 的 `makeLocalProjector(originLat, originLng)`，即本地等距投影（在发射点相切的平面）

**问题**：这两个投影**不完全一致**。在纬度 40°（NYC），Mercator 与本地等距投影在 1.5km 范围内的差异约为 0.1%（约 1-2m）；在纬度 60°（Anchorage），差异约 0.3%（约 4-5m）。对 10m 分辨率地形而言，1-5m 偏移意味着山峰位置可能采样错位。

**决策**：
- `ElevationGrid` 内部按**经纬度**存储元数据（`originLat, originLng, latStep, lngStep`），不用本地米坐标
- `getElevation(lng, lat)` 直接查经纬度，避免任何投影转换误差
- 提供 `getElevationLocal(x, y)` 便利方法：内部先用 `makeLocalProjector.toLatLng(x, y)` 转经纬度，再调 `getElevation`
- 热路径优先用 `getElevation`（省一次转换）

### 8.3 跨瓦片边界处理

**问题**：查询点落在两张瓦片相邻处时，双线性插值需要访问相邻瓦片的边界像素；简单实现会在边界处出现锯齿。

**决策**：`ElevationGrid` 在构造时**先拼接**所有覆盖瓦片的像素为一张大网格，然后统一查询。360KB 的内存足以容纳（§2.3），拼接一次的成本远低于每次查询做边界判断。

`hasCoverage(lng, lat)` 检查查询点是否落在拼接网格边界内。边界外查询返回 `null`（视线延伸到分析范围外，通常意味着已超出兴趣区）。

### 8.4 缓存策略

**决策**：`ElevationGrid` 实例挂在 `hiddenSourceRef.current` 上（跟建筑数据同一生命周期），发射点变化时**整块重取**。理由：

- 每次分析范围只有 4-9 张瓦片，全量重取的网络成本可忽略（<200KB）
- 地形数据无过期问题（不像建筑数据可能更新），但发射点位置变了覆盖范围也变了，复用逻辑复杂度不值得
- 简单胜过 LRU 缓存

若未来观察到用户频繁小幅微调发射点、感知到重取延迟，再引入瓦片级 LRU 缓存。

---

## 9. 风险与降级

### 9.1 数据精度不足

**风险**：AWS Terrain Tiles 10m 分辨率在陡峭微地形（如 Lombard Street 那种极端坡道）可能不够精确，导致小尺度错误。

**缓解**：预留迁移至自建 3DEP PMTiles 的接口；`ElevationGrid` 内部封装数据源差异（对上层暴露 `getElevation(lng, lat)` 一致接口）。

### 9.2 瓦片加载失败

**风险**：S3 宕机、网络中断、CORS 问题等导致 terrain tiles 获取失败。

**决策**（原文档遗漏，需产品选定）：
- **选项 A**：硬失败——显示错误"地形数据不可用，无法完成分析"
- **选项 B**：降级到 z=0 平地假设 + 显式警告"地形数据未加载，分析退化为平地模型"
- **选项 C**：使用最近成功加载的地形网格（可能已过期，但对静态地形而言无碍）

**推荐 B**：降级路径已是本项目实施前的默认行为，用户可继续操作；显式警告避免"结果被静默错误信任"。实施时 UI 层增加一个 error state：`terrainLoadError: true` → ProfilePanel 顶部横幅提示。

### 9.3 垂直基准面

**问题**：不同数据源使用不同高程基准（Vertical Datum）：
- AWS Terrain Tiles / SRTM：**EGM96** 大地水准面
- USGS 3DEP：**NAVD88** 北美垂直基准

美国境内两者差异 20-30m，若第一阶段使用 AWS、后续迁移至 3DEP 而不修正，会出现"建筑与地形突然错位一层楼"的现象。

**决策**：
- 项目内所有高度用作**相对比较**（视线仰角、天际线角），不做绝对海拔展示
- 在同一数据源内相对差异不受基准影响，第一阶段无碍
- `ElevationGrid` 的 `verticalDatum` 字段记录数据源标签；未来跨源使用时先做基准转换

### 9.4 归属署名遗漏

**风险**：AWS Terrain Tiles 派生自 USGS/SRTM/Mapzen，使用条款要求署名。遗漏可能违反 CC BY / USGS 数据使用规定。

**缓解**：阶段 1 实施时同步添加 attribution（§2.4）。

---

## 10. 依赖关系与后续项

### 10.1 与其他 backlog 项的关系

本方案落地在下列 backlog 项之间的位置（依赖顺序，见 `todo.md` 与 `notes.md`）：

1. **方案 A（建筑独立叠层）** —— 已完成（`lib/viewshed/computeRooftopLayer.js`）
2. **空间索引** —— 已完成（`lib/viewshed/buildingIndex.js`）
3. **HiddenBuildingSource（视口无关的建筑加载）** —— 已完成（`lib/HiddenBuildingSource.js`）
4. **本方案：地形高程集成** —— 阶段 1-3
5. **方案 B（径向扫描）** —— 与本方案阶段 4 合并实施
6. **剖面图渲染改进** —— 本方案阶段 5
7. **方案 C（弧长自适应网格）** —— 待定，需在径向扫描落地后重新评估必要性
8. **树木/植被遮挡** —— 独立项，可与本方案并行推进（`todo.md` 中已详细记录）

### 10.2 `todo.md` 联动

`todo.md:40` 的 `地形/DEM 修正地面高程` 条目**保留**，作为总纲入口；正文内容可短化为一句"详见 `地形高程集成_实施方案.md`"。此文档为可执行参考。

### 10.3 涉及但不修改的相关文档

**仓库内**：
- `notes.md`：session 日志。每完成一个阶段追加对应条目（描述改动、决策变化、绕过的问题）。
- `todo.md`：backlog 总纲。§10.2 已说明该条目的联动。

**用户维护的外部参考文档**（非本仓库文件，由用户在会话中作为上下文提供）：
- `可视性数学模型.md`、`烟花可视性数学模型.md`：物理模型定义。本方案不改变数学模型本身，仅补齐数据输入。
- `可视域计算优化方案.md`：早期的 viewshed 优化路线（原方案 A/B/C 分类）。本方案吸收其"径向扫描"部分（即原方案 B）作为 §4.4。
- `地形高程集成_工程设计文档.md`：本方案的源设计文档。用户维护，说明本方案的设计推导过程；实际实施以本文档（`地形高程集成_实施方案.md`）为准。

---

## 附：文件级 impact 一览

| 文件 | 修改类型 | 阶段 |
|---|---|---|
| `components/MapView.jsx` | 新增 raster-dem source + hillshade 图层 + attribution | 1 |
| `lib/LayerManager.js` | `addSources` 扩展 | 1 |
| `lib/viewshed/ElevationGrid.js` | **新增** | 2 |
| `lib/HiddenBuildingSource.js` | 重构或扩展为同时管理 terrain | 2 |
| `lib/viewshed/worker.js` | 新增 terrain fetch/decode 消息处理 | 2 |
| `lib/viewshed/sightline.js` | 语义调整为绝对海拔（签名不变） | 3 |
| `lib/viewshed/computeViewshed.js` | 传入 terrainGrid，高度基准改绝对 | 3 |
| `lib/viewshed/computeRooftopLayer.js` | 建筑地基取地形海拔 | 3 |
| `lib/viewshed/computeProfile.js` | 加 `terrainProfile` 输出 | 3, 5 |
| `components/launch/LaunchPointControl.jsx` | 传递 terrainGrid、调整参数构造、更新免责声明 | 3, 5 |
| `components/analysis/ProfilePanel.jsx` | SightlineChart 加地形填充，更新免责声明 | 5 |
| `__tests__/ElevationGrid.test.js` | **新增** | 2 |
| `__tests__/computeViewshedTerrain.test.js` | **新增** | 3, 4 |
| 现有 `__tests__/*.test.js` | Fixture 加 `terrainGrid.flat()` 参数 | 3 |
| `todo.md` | 更新终止条 | 5 |
| `notes.md` | 每阶段完成后追加日志 | 每阶段 |

---

**文档结束。**下一步：实施阶段 1（展示层 + attribution），预计 0.5-1 天。开始前确认已有 AWS Terrain Tiles 访问权（S3 公共 endpoint，无需注册）。
